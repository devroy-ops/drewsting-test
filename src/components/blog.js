import React, { useCallback, useContext } from 'react'

export default function About() {
  return (
    <div style={{marginBottom: "5em", marginTop: "5em"}} className="container text-light">
        <h2>Drawstring Blog</h2>
    </div>
  )
}
